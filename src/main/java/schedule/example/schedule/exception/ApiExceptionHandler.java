package schedule.example.schedule.exception;

/**
 * @deprecated Superseded by {@link GlobalExceptionHandler}. This file is retained only
 *             because Java requires the public class name to match the filename. All
 *             exception-handling logic has been moved to {@link GlobalExceptionHandler}.
 *             Do not add handlers here.
 */
@Deprecated(since = "2.0", forRemoval = true)
class ApiExceptionHandler {
	// Intentionally empty — see GlobalExceptionHandler for all handlers.
}